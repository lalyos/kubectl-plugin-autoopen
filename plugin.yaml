name: "autoopen"                 # REQUIRED: the plugin command name, to be invoked under 'kubectl'
shortDesc: "svc autoopen"    # REQUIRED: the command short description, for help
longDesc: "Automatically opens NodePort services in browse"                      # the command long description, for help
example: ""                       # command example(s), for help
command: "./autoopen"             # REQUIRED: the command, binary, or script to invoke when running the plugin
flags:                            # flags supported by the plugin
  - name: "debug"                  # REQUIRED for each flag: flag name
#    shorthand: "h"                # short version of the flag name
    desc: "prints commands"             # REQUIRED for each flag: flag description
    defValue: "false"   
